package user

// Making User struct public by starting with capital letter so can be used in other packages
type User struct {
	// Making fields public by starting with capital letter so can be used in other packages
	Name  string
	Email string
}
